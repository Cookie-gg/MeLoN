name: Deploy to Google AppEngine

on:
  push:
    branches:
      - master

jobs:
  deploy:
    name: Deploy to Google AppEngine

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Create dot env file
        shell: bash
        run: |
          touch .env.production
          echo "FRONT_URL='${{ secrets.FRONT_URL }}'\nMONGODB_KEY='${{ secrets.MONGODB_KEY }}'\nGRAPHQL_SECRET_KEY='${{ secrets.GRAPHQL_SECRET_KEY }}'\n" >> .env.production
          echo "MONGODB_KEY='${{ secrets.MONGODB_KEY }}'\nAUTH_SECRET_KEY='${{ secrets.AUTH_SECRET_KEY }}'\nICON_SECRET_KEY='${{ secrets.ICON_SECRET_KEY }}'\nMARKDOWN_SECRET_KEY='${{ secrets.MARKDOWN_SECRET_KEY }}'\nMAIL_SECRET_KEY='${{ secrets.MAIL_SECRET_KEY }}'\nADMIN_NAME='${{ secrets.ADMIN_NAME }}'\nADMIN_PASSWORD='${{ secrets.ADMIN_PASSWORD }}'\nMAIL_USER='${{ secrets.MAIL_USER }}'\nMAIL_PASS='${{ secrets.MAIL_PASS }}'\nGH_OWNER='${{ secrets.GH_OWNER }}'\n" >> .env.production
          echo "ICON_SECRET_KEY='${{ secrets.ICON_SECRET_KEY }}'\nGH_ACCESS_TOKEN='${{ secrets.GH_ACCESS_TOKEN }}'\nTWITTER_AUTH='${{ secrets.TWITTER_AUTH }}'\n" >> .env.production

      - name: Setup Node.js
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - name: Build Nest.js application
        run: |
          npm i
          npm run build

      - name: Setup gcloud
        uses: 'google-github-actions/auth@v0'
        with:
          credentials_json: '${{ secrets.GCP_SA_KEY }}'

      - name: 'Deploy to App Engine'
        uses: 'google-github-actions/deploy-appengine@v0'
